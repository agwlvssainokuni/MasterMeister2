# マスタデータメンテナンスアプリケーション 要件定義書

## 1. プロジェクト概要

RDBMS に格納されたマスタデータをメンテナンスするための Web アプリケーションを開発する。
Spring Boot バックエンドと React フロントエンドによる SPA として構築する。

- **開発体制**: 単独開発者による初期開発
- **開発方針**: MVP ファーストで段階的に機能を追加する
- **優先順位**: ユーザ管理 → 対象 RDBMS セットアップ → アクセス制御 → データ表示、の順にワークフロー上流から実装する

## 2. 技術スタック

| 項目 | 内容 |
|---|---|
| Java | 25 (最新 LTS。次期 LTS 登場時に移行) |
| Node.js | 24 (最新 LTS。次期 LTS 登場時に移行) |
| バックエンド | Spring Boot 4.1 |
| フロントエンド | React 19 |
| ビルドツール（バックエンド） | Gradle 9.6 (最新版) |
| ビルドツール（フロントエンド） | Vite |
| DB アクセス（内部 DB） | JPA |
| DB アクセス（対象 RDBMS） | NamedParameterJdbcTemplate |
| 内部データベース | H2 Database |
| コネクションプール | DriverManager.getConnection() ではなく、対象 RDBMS 用に DB コネクションプールを使用 |
| 日時型の扱い | 対象 RDBMS の日時データには java.time API を使用 |

### 対象 RDBMS（メンテナンス対象として必須サポート）
- MySQL
- MariaDB
- PostgreSQL
- H2 Database

## 3. データベースアーキテクチャ

- **内部 DB**: アプリケーションの運用データ（ユーザ、権限設定、保存クエリ、実行履歴、監査ログ等）を格納する専用データベース
- **対象 RDBMS**: メンテナンス対象のマスタデータを格納するデータベース
- **コネクションプール**: 対象 RDBMS への接続パラメータは接続設定の一部として構成する

## 4. プロジェクト構成

```
MasterMeister/
├── backend/        # Spring Boot アプリケーション
├── frontend/       # React アプリケーション (Vite)
└── devenv/         # 開発環境 (Docker Compose)
```

### 開発環境 (devenv/ 配下の Docker Compose)
- **メールサーバ**: MailPit（開発時のメール送受信確認用）
- **開発用データベース**:
  - MySQL コンテナ
  - MariaDB コンテナ
  - PostgreSQL コンテナ
  - H2（組み込みのためコンテナ不要）

## 5. アプリケーション機能要件

### 5.1 ユーザ登録

2 段階（メールアドレス先行方式）の登録フローとする。

1. ユーザがメールアドレスを送信し、登録を開始する
2. 新規ユーザにのみ、有効期限付きリンクを含む登録確認メールを送信する
3. ユーザはメール内のリンクからパスワードを設定して登録を完了する
4. 登録完了したユーザは管理者ダッシュボードに一覧表示される

**管理者承認ワークフロー**
- 管理者が登録完了ユーザを承認または却下する
- 承認・却下結果をユーザにメール通知する
- 承認済みユーザのみログイン可能

**セキュリティ対策**
- メールアドレス列挙攻撃対策（ユーザの存在有無にかかわらず一貫した API レスポンスを返す）
- 登録用トークンの有効期限は設定可能（デフォルト 3 時間）
- 登録処理専用のトークン管理の仕組みを設ける

**設定項目**
- トークン有効期限: `mm.app.user-registration.token-expiry-hours`
- 登録用パス・トークンパラメータ名はカスタマイズ可能

### 5.2 対象 RDBMS セットアップ

- 管理者が管理者ダッシュボードで対象 RDBMS の接続情報を入力する
- スキーマ更新操作により対象 RDBMSに接続し、テーブル/ビュー構造（物理名、コメント、型、制約）を読み取り内部 DB に取り込む
- 管理者は取り込んだ対象 RDBMS 構造に基づきアクセス権限を設定する

**アクセス権限モデル**
- **テーブルレベル**: Allow（表示）/ Deny（非表示）
- **カラムレベル**:
  - アクセス不可
  - 読み取りのみ (R)
  - 既存レコードの読み取り／更新 (RU)
  - 全カラムが更新可能な場合はフルアクセス (CRUD)

- アクセス権限設定は YAML 形式でエクスポート／インポート可能とする

### 5.3 ユーザ認証

- ユーザ（管理者含む）はログインしてから各機能を利用する

### 5.4 マスタメンテナンス機能

- **テーブル/ビュー一覧**: トップ画面にアクセス可能なテーブル/ビューを表示
- **レコード一覧**: テーブル/ビュー選択時にページング付きレコード一覧を表示
- **絞り込み**: 読み取り権限以上を持つカラムのみを対象に、フィルタ条件・ソートを直感的な UI で指定可能
- **SQL 入力**: WHERE 句・ORDER BY 句の手入力（権限に関係なく利用可能）
- **レコード編集**: レコード一覧画面上で、更新権限のあるカラムの値を編集
- **トランザクション管理**: 「反映」ボタン押下で、変更内容を単一トランザクションとしてバックエンド API に送信
- **レコード操作**: フルアクセス権限がある場合、レコードの作成・削除が可能
- **統一 API**: 作成・更新・削除のすべての操作を単一トランザクションとして扱う API エンドポイントを 1 つに統合する

### 5.5 クエリビルダー機能

- **タブ構成**: SELECT / FROM / JOIN / WHERE / GROUP BY / HAVING / ORDER BY / LIMIT OFFSET
- **テーブル選択**: FROM/JOIN タブでテーブルとエイリアス（任意）を指定
- **カラム選択**: 他タブでは選択済みテーブル/エイリアスに対しアクセス可能なカラムを表示
- **集計関数**: SELECT / HAVING / ORDER BY タブで使用可能
- **SQL 生成**: UI での指定内容から SQL を生成
- **連携**: 生成した SQL は保存または直接実行が可能
- **リバースエンジニアリング**: 他機能からの遷移時、SQL を解析してクエリビルダーの各タブに反映する

### 5.6 クエリ保存機能

- **名前付き保存**: 手入力またはクエリビルダーで作成した SQL に任意の名前を付けて保存
- **公開範囲**: 公開（Public）/ 非公開（Private）を選択可能
- **クエリ実行**: 保存済みクエリはユーザが実行可能
- **編集権限**: 保存クエリの編集は作成者のみ可能

### 5.7 クエリ実行機能

- **SQL 入力/編集**: 実行画面で SQL を入力・編集可能
- **実行制限**: 読み取り専用 SQL のみ実行可能
- **パラメータ対応**: NamedParameterJdbcTemplate による ":param" 形式のパラメータ化クエリをサポート
- **パラメータ入力**: パラメータを自動検出し、値を設定する UI を提供
- **ページング**: ページングの有無を設定可能
- **履歴記録**: SQL、パラメータ、結果件数、実行時間、実行回数（保存クエリの場合）、実行日時、実行者を記録
- **結果表示**: 表形式で、必要に応じてページングして表示
- **保存クエリ実行時の制限**: 保存クエリを実行する場合は編集不可

### 5.8 クエリ履歴機能

- **履歴一覧**: クエリ実行履歴をページング付きで表示
- **種別表示**: 保存クエリと直接入力クエリを区別して表示
- **絞り込み**:
  - 実行日時（範囲指定）
  - 実行者（全ユーザ／自分のみ）
  - SQL テキスト検索
- **画面遷移**: 履歴からクエリ実行・クエリ保存・クエリビルダーへ遷移可能

## 6. 監査ログ要件

### 6.1 必須記録イベント

**認証イベント**
- ログイン（日時、ユーザ名、IP アドレス）
- ログアウト（日時、ユーザ名、セッション継続時間）
- ログイン失敗（日時、試行されたユーザ名、IP アドレス）

**管理操作**
- アカウント登録申請
- 管理者によるアカウント承認／却下
- 対象 RDBMS 接続設定の変更
- スキーマ取り込み操作とその結果
- アクセス権限設定（テーブル/カラムレベル）の変更

**データアクセスイベント**
- 大量データ取得（閾値は設定可能、デフォルト 100 件以上）
- クエリ実行（結果件数・実行時間を含む）
- データ更新操作（挿入・更新・削除）

### 6.2 監査ログの仕様

- **形式**: 構造化ログ（JSON 推奨）
- **保存先**: 内部データベースに永続化
- **記録項目**: 日時（ISO 8601）、ユーザ ID、操作種別、対象リソース、IP アドレス、結果ステータス
- **保持期間**: 設定可能
- **アクセス**: 管理者のみ閲覧可能

### 6.3 セキュリティ・コンプライアンス

- 特別な要件はなし。標準的な Web アプリケーションのセキュリティ対策に準拠する
- ログ中の機微情報は基本的な保護を行う
- 監査ログへのアクセスは管理者のみに限定する

## 7. 非機能要件

### 7.1 性能・規模

- **同時利用者数**: 約 10 名を想定
- **大量データ閾値**: 監査ログ対象となるレコード件数の閾値は設定可能（デフォルト 100 件以上）
- **応答性能**: 典型的なマスタデータ操作に対して妥当な性能を確保する

### 7.2 デプロイ

**主デプロイ形態**
- Twelve-Factor App の原則に準拠する
- 自己完結型の実行可能 WAR ファイルを生成する
- すべての設定は環境変数経由で行う
- コンテナ環境に適した構造化ログを出力する

**その他のデプロイ形態**
- 実行可能 WAR から Docker コンテナ化をサポートする
- Tomcat への WAR デプロイにも対応する（将来対応）

### 7.3 メール送信

- **開発環境**: MailPit コンテナを用いてメール送受信を確認する
- **本番環境**: 環境変数経由で SMTP 設定を構成可能とする